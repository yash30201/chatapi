const jwt = require('jsonwebtoken');

const decode = function(req, res, next){
    // DO something
}

const encode = function(req, res, next){
    // Do something
}

module.exports = {
    decode : decode,
    encode : encode
}